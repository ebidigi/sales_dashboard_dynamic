# 稼働報酬チーム管理DB - 要件定義書

## 1. システム概要

稼働報酬チーム専用の営業KPIダッシュボード（SPA）。
GAS API経由でTurso DBの実績データを取得し、ブラウザからリアルタイムに進捗を可視化する。

## 2. データフロー

```
スプレッドシート（実績rawdata）
  ↓ GAS定期同期（既存）
  ↓ member_name_aliases でメンバー名を正規化
Turso DB（all-staff-rawdata-ebidigi）
  ↓ GAS API（Code.js → Turso HTTP API）
  ↓ JSON レスポンス
ダッシュボード（index.html）
```

> 成果報酬DBはフロントエンド→Turso直接クエリだが、
> 稼働報酬は既存のGAS API中間層を維持する（認証情報をブラウザに露出しない）。

## 3. Turso DB（共用）

- **URL**: `https://all-staff-rawdata-ebidigi.aws-ap-northeast-1.turso.io`
- **使用テーブル**:

| テーブル | 用途 |
|----------|------|
| `performance_rawdata` | 日次実績（架電数、PR数、アポ数、稼働時間） |
| `targets` | 月次目標（担当者×案件×月） |
| `members` | 担当者マスタ |
| `projects` | 案件マスタ |
| `member_name_aliases` | member_name揺れの正規化マッピング |
| `settings` | KV設定（`sales_target_YYYY-MM`等） |

> `deals` テーブルは案件管理タブ廃止に伴い、本ダッシュボードでは使用しない。

## 4. ダッシュボード機能

### 設計方針

- **チーム概念なし**: 担当者は横並びで表示（チーム単位の集計・カードは不要）
- **2つの軸**: 「担当者ごと」と「案件ごと」の両軸で集計・表示
- **売上表示あり**: 案件管理タブは廃止するが、売上実績は朝礼タブで表示
- **歩留まりベンチマークなし**: 基準値との比較は行わない（実数値のみ表示）

### 現状 → 変更後のタブ構成

| 現状 | 変更後 | 備考 |
|------|--------|------|
| 概要 | **朝礼** | 利用シーンに合わせてリデザイン |
| 分析 | **詳細分析** | 歩留まり・個人分析を追加して拡張 |
| 案件管理 | **廃止** | |
| 設定 | **設定**（簡易化） | 売上目標設定のみ残す |

---

### 4.1 朝礼タブ（旧: 概要タブ）

**利用シーン**: 毎朝の朝礼で全体進捗を確認・発表

#### 全体KPIバー

| 指標 | 内容 |
|------|------|
| 売上進捗 | 売上実績 / 売上目標（進捗バー + ラップ目標ライン） |
| 架電進捗 | 実績 / 目標（進捗バー + ラップ目標ライン） |
| アポ進捗 | 実績 / 目標（進捗バー + ラップ目標ライン） |
| 架電toアポ率 | 全体の転換率 |
| 進捗差分 | アポ進捗 − 標準進捗（乖離の可視化） |

- **ラップ目標** = 月間目標 × (経過営業日 / 全営業日)
- 進捗バーにラップ目標を縦線で表示
- 色分け: 青（ラップ以上）/ 黄（80%以上）/ 赤（80%未満）

#### アラート

- 架電ラップ未達の場合に警告表示
- アポラップ未達の場合に警告表示

#### 担当者カード

担当者ごとに1カード。案件横断で集計した値を表示:

| 項目 | 内容 |
|------|------|
| 担当者名 | ヘッダー |
| 売上 | 実績金額 |
| 架電進捗 | 実績/目標 + 進捗バー + ペース% |
| アポ進捗 | 実績/目標 + 進捗バー + ペース% |
| 架電/H | 実績 vs 目標 |
| 架電toアポ率 | 実績% |

- ペース = 実績 / (目標 × 標準進捗率)
- カードの並び順: アポ進捗率の降順

#### 案件別グラフ

- 架電進捗 vs アポ進捗（横棒グラフ、標準進捗ライン付き）
- 進捗率ランキング（案件別）

#### 個人別グラフ

- 架電進捗 vs アポ進捗（横棒グラフ）
- 進捗率ランキング（個人別）

#### 比較基準トグル（維持）

- 前日終了時 / 本日時点 / 明日時点 の3段階切替
- 月セレクター（前月・当月の切替）

---

### 4.2 詳細分析タブ（旧: 分析タブを拡張）

**利用シーン**: 週次振り返り、個人の改善

3つのサブビュー（タブ内切り替え）:

#### サブビュー A: 実績テーブル

- **日次 / 週次 / 月次** の切替
- フィルター: 案件・担当者・期間
- KPI表示（実数指標）: 架電数 / PR数 / アポ数 / 架電数/H
- トレンドグラフ: 7種（架電数 / PR数 / アポ数 / 架電toPR率 / PRtoアポ率 / 架電toアポ率 / 架電数/H）
- 前月比較トグル（既存機能を維持）

#### サブビュー B: 歩留まり（新規）

- **ファネル分析**: 架電 → PR → アポ の転換ビジュアル
- **担当者別レート比較テーブル**: 架電toPR率 / PRtoアポ率 / 架電toアポ率 / 架電数/H
- **案件別歩留まりテーブル**: 案件ごとの転換率一覧

#### サブビュー C: 個人分析（新規）

- 担当者選択 → 案件別の実績テーブル
- 目標 vs 実績 vs 達成率
- 歩留まり: 架電toPR / PRtoアポ / 架電toアポ
- トレンドグラフ（個人の日次推移）

---

### 4.3 設定タブ（簡易化）

- 月別売上目標設定（既存機能を維持）
- ※担当者×案件の月次目標は `targets` テーブルに直接管理

---

## 5. 廃止する機能

| 機能 | 理由 |
|------|------|
| 案件管理タブ全体 | 不要 |
| パイプライン（提案前/提案済み/見積もり等） | 不要 |
| 受注案件一覧 | 不要 |
| 案件の追加/編集/削除フォーム | 不要 |
| 目標対進捗サマリー（Q単位） | 不要 |
| チーム売上カード | deals依存のため廃止 |
| `deals` テーブルの読み書き | 不要 |
| `pipeline_v2` API | 不要 |
| `deal_upsert` / `deal_delete` API | 不要 |
| `sales_report_rawdata` クエリ（未コミット変更） | テーブル未確認・バグの原因 |

## 6. GAS API（変更後）

### GET エンドポイント

| type | 用途 | 維持/廃止 |
|------|------|-----------|
| `monthly` | 朝礼タブデータ | **維持** |
| `rawdata` | 詳細分析タブデータ | **維持**（週次/月次集計を追加） |
| `sales_targets` | 設定タブ読み取り | **維持** |
| `pipeline_v2` | 案件管理 | **廃止** |
| `deal_upsert` (GET) | 案件更新 | **廃止** |
| `deal_delete` (GET) | 案件削除 | **廃止** |
| `sales_targets_save` (GET) | 設定保存 | **廃止**（POSTのみに） |
| `target_upsert` (GET) | 目標更新 | **廃止**（POSTのみに） |

### POST エンドポイント

| type | 用途 | 維持/廃止 |
|------|------|-----------|
| `sales_targets_save` | 設定保存 | **維持** |
| `target_upsert` | 目標更新 | **維持** |
| `deal_upsert` | 案件更新 | **廃止** |
| `deal_delete` | 案件削除 | **廃止** |

## 7. 営業日計算・ビジネスロジック

- **営業日**: GoogleカレンダーAPI（ja.japanese#holiday）で祝日取得、土日除外
- **標準進捗率** = 経過営業日 / 全営業日 × 100
- **ラップ目標** = 月間目標 × 標準進捗率
- **ペース** = 実績 / (目標 × 標準進捗率)
- **歩留まりベンチマーク**（要確認）:
  - 架電toPR率: TBD%
  - PRtoアポ率: TBD%
  - 架電toアポ率: TBD%
  - 架電数/H: TBD

## 8. 今回のバグ修正（先行対応）

| バグ | 原因 | 修正方針 |
|------|------|----------|
| "Cannot read properties of undefined (reading 'name')" | `renderAnalysis()` で members が空配列の場合に `sort()[0]` が undefined | 空配列ガードを追加 |
| `sales_report_rawdata` クエリエラー | 未コミット変更で存在不明テーブルを参照 | 該当クエリを削除 |
| doGet に書き込み系 API 追加 | 未コミット変更で GET に upsert/delete を追加 | 削除（POST のみに戻す） |

## 9. 実装フェーズ

### Phase 1: バグ修正 + クリーンアップ
- 未コミット変更の `sales_report_rawdata` 関連を除去
- `renderAnalysis()` の空配列ガード追加
- 案件管理タブ・関連コード除去
- doGet の書き込み系エンドポイント除去
- GAS側の不要関数除去（pipeline_v2, deal系）
- サイドバーから「案件管理」を除去

### Phase 2: 朝礼タブ刷新
- タブ名を「概要」→「朝礼」に変更
- チーム売上カードを廃止、架電/アポKPIバーに変更
- ラップ目標ラインの追加
- アラート表示の追加
- 担当者カードのUI改善
- グラフの維持・改善

### Phase 3: 詳細分析タブ拡張
- サブビュー切替UI追加（実績テーブル / 歩留まり / 個人分析）
- 日次/週次/月次切替
- 歩留まりビュー（ファネル + レート比較テーブル + 診断）
- 個人分析ビュー
- GAS API: rawdata に週次/月次集計オプションを追加
